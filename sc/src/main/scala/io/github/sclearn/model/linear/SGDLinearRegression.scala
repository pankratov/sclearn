package io.github.sclearn.model.linear

import io.github.sclearn.Dataset

class SGDLinearRegression[T] extends LinearRegression[T] {
  def fit(x: Dataset[T], y: Dataset[T]): Unit = {
    // x.map()
    
    ???
  }
}